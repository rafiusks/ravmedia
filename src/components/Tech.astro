---
const { img, title, pill } = Astro.props;
---

<div class="flex items-center space-x-2 p-2 rounded-lg hover:bg-gray-50 transition-colors">
	<img class="w-10" src={img} alt={title} />
	<span class="font-medium text-gray-900">{title}</span>
</div>
